<%- contentFor('css') %>
<style>
    .change-password-page {
        display: flex;
        flex-direction: column;
        gap: 24px;
    }

    .change-password-card {
        background: #ffffff;
        border-radius: 16px;
        padding: 28px;
        box-shadow: 0 18px 35px -25px rgba(15, 23, 42, 0.35);
    }

    .change-password-card header {
        display: flex;
        flex-direction: column;
        gap: 8px;
        margin-bottom: 24px;
    }

    .change-password-card header h2 {
        font-size: 1.5rem;
        margin: 0;
    }

    .change-password-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        font-size: 0.95rem;
        color: #64748b;
    }

    .change-password-form {
        display: grid;
        gap: 18px;
    }

    .change-password-field {
        display: grid;
        gap: 8px;
    }

    .change-password-field label {
        font-weight: 600;
        color: #1f2937;
    }

    .password-input-group {
        position: relative;
        display: flex;
        align-items: center;
        border: 1px solid #e2e8f0;
        border-radius: 12px;
        background: #f8fafc;
        transition: border-color 0.2s ease, box-shadow 0.2s ease;
    }

    .password-input-group:focus-within {
        border-color: #6366f1;
        box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.12);
        background: #ffffff;
    }

    .password-input-group input {
        flex: 1;
        width: 100%;
        border: none;
        background: transparent;
        padding: 12px 48px 12px 16px;
        font-size: 1rem;
        color: #0f172a;
    }

    .password-input-group input:focus {
        outline: none;
    }

    .password-visibility-toggle {
        position: absolute;
        right: 12px;
        top: 50%;
        transform: translateY(-50%);
        border: none;
        background: none;
        color: #6366f1;
        font-weight: 600;
        cursor: pointer;
        padding: 6px 10px;
    }

    .change-password-hint {
        font-size: 0.9rem;
        color: #64748b;
        display: flex;
        gap: 10px;
        align-items: center;
    }

    .change-password-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        justify-content: flex-end;
        margin-top: 8px;
    }

    .change-password-actions .btn {
        border-radius: 12px;
        padding: 10px 18px;
    }

    .change-password-actions .btn-light {
        background: #e2e8f0;
        color: #1e293b;
        border: 1px solid transparent;
    }

    .change-password-actions .btn-light:hover {
        background: #cbd5f5;
        color: #1e293b;
    }

    .password-policy {
        background: #f1f5f9;
        border-radius: 12px;
        padding: 12px 16px;
        font-size: 0.9rem;
        color: #475569;
    }

    @media (max-width: 768px) {
        .change-password-card {
            padding: 20px;
        }

        .change-password-actions {
            flex-direction: column;
            align-items: stretch;
        }

        .change-password-actions .btn {
            width: 100%;
        }
    }
</style>

<%- contentFor('js') %>
<script>
    document.addEventListener('DOMContentLoaded', function () {
        var toggles = document.querySelectorAll('[data-password-toggle]');

        toggles.forEach(function (toggle) {
            toggle.addEventListener('click', function () {
                var targetId = toggle.getAttribute('data-password-toggle');
                var field = document.getElementById(targetId);

                if (!field) {
                    return;
                }

                var isHidden = field.getAttribute('type') === 'password';
                field.setAttribute('type', isHidden ? 'text' : 'password');
                toggle.textContent = isHidden ? 'Ẩn' : 'Hiện';
                toggle.setAttribute('aria-label', isHidden ? 'Ẩn mật khẩu' : 'Hiện mật khẩu');
            });
        });
    });
</script>

<%
    const activeUser = user || {};
    const nameLabel = activeUser.fullName || activeUser.full_name || activeUser.username || 'Người dùng';
    const passwordMinLength = Number.isInteger(minPasswordLength) ? minPasswordLength : 6;
%>

<div class="change-password-page">
    <section class="page-header">
        <div class="page-header__intro">
            <h1>Đổi mật khẩu</h1>
            <p>Bảo vệ tài khoản của bạn bằng một mật khẩu mạnh và chỉ bạn biết.</p>
        </div>
    </section>

    <section class="change-password-card" aria-labelledby="change-password-heading">
        <header>
            <div class="change-password-meta">
                <span>Đang đăng nhập: <strong><%= nameLabel %></strong></span>
                <span>Yêu cầu tối thiểu: <strong><%= passwordMinLength %> ký tự</strong></span>
            </div>
            <h2 id="change-password-heading">Cập nhật mật khẩu đăng nhập</h2>
            <p class="change-password-hint">
                <span aria-hidden="true">🔒</span>
                Đừng chia sẻ mật khẩu của bạn và tránh sử dụng lại mật khẩu ở các hệ thống khác.
            </p>
        </header>

        <form class="change-password-form" action="/auth/change-password" method="post" novalidate>
            <div class="change-password-field">
                <label for="current_password">Mật khẩu hiện tại</label>
                <div class="password-input-group">
                    <input type="password" id="current_password" name="current_password" autocomplete="current-password" minlength="1" required>
                    <button type="button" class="password-visibility-toggle" data-password-toggle="current_password">Hiện</button>
                </div>
            </div>

            <div class="change-password-field">
                <label for="new_password">Mật khẩu mới</label>
                <div class="password-input-group">
                    <input type="password" id="new_password" name="new_password" autocomplete="new-password" minlength="<%= passwordMinLength %>" required>
                    <button type="button" class="password-visibility-toggle" data-password-toggle="new_password">Hiện</button>
                </div>
                <div class="password-policy">
                    Sử dụng tối thiểu <%= passwordMinLength %> ký tự và kết hợp chữ hoa, chữ thường, số hoặc ký tự đặc biệt để tăng bảo mật.
                </div>
            </div>

            <div class="change-password-field">
                <label for="confirm_password">Xác nhận mật khẩu mới</label>
                <div class="password-input-group">
                    <input type="password" id="confirm_password" name="confirm_password" autocomplete="new-password" minlength="<%= passwordMinLength %>" required>
                    <button type="button" class="password-visibility-toggle" data-password-toggle="confirm_password">Hiện</button>
                </div>
            </div>

            <div class="change-password-actions">
                <a class="btn btn-light" href="/dashboard">Hủy</a>
                <button class="btn btn-primary" type="submit">Cập nhật mật khẩu</button>
            </div>
        </form>
    </section>
</div>
