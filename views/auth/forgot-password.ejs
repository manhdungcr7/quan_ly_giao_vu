<%- contentFor('content') %>
<div class="auth-wrapper">
    <div class="auth-card shadow-lg">
        <div class="auth-side d-none d-lg-flex forgot-password-side">
            <div class="auth-side-inner">
                <span class="badge bg-light text-dark mb-3 px-3 py-2 fw-semibold">
                    <i class="fas fa-key me-2 text-warning"></i>Khôi phục truy cập
                </span>
                <h2 class="display-6 fw-semibold text-white mb-3">Đặt lại mật khẩu trong vài bước</h2>
                <p class="text-white-50 mb-4">Hệ thống sẽ xác nhận email làm việc của bạn để hỗ trợ khôi phục truy cập an toàn.</p>
                <ul class="text-white-50 small list-unstyled mb-0">
                    <li class="mb-2"><i class="fas fa-shield-alt me-2 text-warning"></i>Thông tin của bạn được mã hóa theo tiêu chuẩn nội bộ.</li>
                    <li class="mb-2"><i class="fas fa-headset me-2 text-warning"></i>Đội ngũ quản trị sẽ hỗ trợ nếu bạn cần trợ giúp bổ sung.</li>
                    <li><i class="fas fa-clock me-2 text-warning"></i>Phản hồi thường được gửi trong giờ làm việc.</li>
                </ul>
            </div>
        </div>
        <div class="auth-form p-4 p-md-5">
            <div class="mb-4 text-center text-md-start">
                <h3 class="fw-bold text-warning mb-1">Quên mật khẩu</h3>
                <p class="text-muted mb-0">Nhập email đã đăng ký để chúng tôi kiểm tra và hướng dẫn bước tiếp theo.</p>
            </div>

            <%- include('../partials/alerts') %>

            <form action="/auth/forgot-password" method="POST" class="needs-validation" novalidate>
                <div class="mb-4">
                    <label for="email" class="form-label fw-semibold">Email đăng ký tài khoản</label>
                    <div class="input-group input-group-lg">
                        <span class="input-group-text bg-light border-end-0"><i class="fas fa-envelope text-secondary"></i></span>
                        <input type="email" class="form-control border-start-0" id="email" name="email" placeholder="email@hocvien.edu.vn" required>
                    </div>
                    <div class="form-text mt-2">Vui lòng sử dụng email nội bộ hoặc email đã được hệ thống phê duyệt.</div>
                </div>

                <div class="d-grid">
                    <button type="submit" class="btn btn-warning btn-lg text-dark">
                        <i class="fas fa-paper-plane me-2"></i>Gửi yêu cầu hỗ trợ
                    </button>
                </div>
            </form>

            <div class="mt-4 pt-3 border-top text-center">
                <a href="/auth/login" class="text-decoration-none fw-semibold"><i class="fas fa-arrow-left me-2"></i>Quay về trang đăng nhập</a>
            </div>

            <div class="mt-4 small text-muted text-center">
                <i class="fas fa-info-circle me-1"></i>Nếu không nhận được phản hồi sau 24 giờ, vui lòng liên hệ trực tiếp quản trị viên hệ thống.
            </div>
        </div>
    </div>
</div>

<%- contentFor('css') %>
<style>
    .auth-wrapper {
        width: 100%;
        max-width: 960px;
    }

    body.auth-layout {
        background: radial-gradient(circle at top left, rgba(252, 211, 77, 0.16), transparent 42%),
                    radial-gradient(circle at bottom right, rgba(217, 119, 6, 0.18), transparent 48%),
                    linear-gradient(135deg, #fef3c7 0%, #f8fafc 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 40px 16px;
    }

    .auth-card {
        background: #ffffff;
        border-radius: 28px;
        overflow: hidden;
        display: grid;
        grid-template-columns: minmax(0, 420px) minmax(0, 1fr);
        position: relative;
    }

    .auth-side {
        background: linear-gradient(135deg, #f59e0b, #d97706);
        padding: 48px;
        position: relative;
        isolation: isolate;
    }

    .auth-side::after {
        content: '';
        position: absolute;
        inset: 24px;
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 24px;
        opacity: 0.75;
        z-index: -1;
    }

    .auth-side-inner {
        max-width: 320px;
    }

    .auth-form .form-control {
        border-radius: 16px;
        padding: 0.85rem 1rem;
    }

    .auth-form .input-group-text {
        border-radius: 16px 0 0 16px;
    }

    .auth-form .form-control:focus {
        box-shadow: 0 0 0 0.2rem rgba(217, 119, 6, 0.18);
        border-color: rgba(217, 119, 6, 0.45);
    }

    .auth-form .btn-warning {
        border-radius: 16px;
        background: linear-gradient(135deg, #f59e0b, #fbbf24);
        border: none;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .auth-form .btn-warning:hover {
        transform: translateY(-1px);
        box-shadow: 0 15px 30px -12px rgba(217, 119, 6, 0.45);
    }

    @media (max-width: 992px) {
        body.auth-layout {
            padding: 32px 12px;
        }

        .auth-card {
            grid-template-columns: 1fr;
        }

        .auth-side {
            display: none !important;
        }
    }

    @media (max-width: 576px) {
        body.auth-layout {
            padding: 24px 12px;
        }

        .auth-form .input-group {
            flex-direction: column;
        }

        .auth-form .input-group-text {
            width: 100%;
            border-radius: 14px;
            margin-bottom: 6px;
        }

        .auth-form .form-control {
            border-radius: 14px;
        }
    }
</style>

<%- contentFor('js') %>
<script>
document.addEventListener('DOMContentLoaded', function(){
    document.body.classList.add('auth-layout');
});
</script>
