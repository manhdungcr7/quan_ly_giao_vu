<%- contentFor('content') %>
<div class="auth-wrapper">
    <div class="auth-card shadow-lg">
        <div class="auth-side d-none d-md-flex">
            <div class="auth-side-inner">
                <span class="badge bg-light text-dark mb-3 px-3 py-2 fw-semibold">
                    <i class="fas fa-graduation-cap me-2 text-primary"></i>Hệ thống Quản lý Giáo vụ
                </span>
                <h2 class="display-6 fw-semibold text-white mb-3">Kết nối nhanh tới dữ liệu học vụ</h2>
                <p class="text-white-50 mb-4">Theo dõi tiến độ, quản lý hồ sơ và phối hợp với đội ngũ giảng viên trong một nền tảng thống nhất.</p>
                <ul class="list-unstyled text-white-50 small mb-0">
                    <li class="mb-2"><i class="fas fa-check-circle me-2 text-success"></i>Bảo mật phiên đăng nhập nâng cao</li>
                    <li class="mb-2"><i class="fas fa-check-circle me-2 text-success"></i>Thống kê trực quan theo thời gian thực</li>
                    <li><i class="fas fa-check-circle me-2 text-success"></i>Hỗ trợ toàn bộ quy trình giáo vụ</li>
                </ul>
            </div>
        </div>
        <div class="auth-form p-4 p-md-5">
            <div class="mb-4 text-center text-md-start">
                <h3 class="fw-bold text-primary mb-1">Đăng nhập hệ thống</h3>
                <p class="text-muted mb-0">Chào mừng trở lại! Vui lòng nhập thông tin để tiếp tục.</p>
            </div>
            <form action="/auth/login" method="POST" class="needs-validation" novalidate>
                <div class="mb-3">
                    <label for="username" class="form-label fw-semibold">Tên đăng nhập</label>
                    <div class="input-group input-group-lg">
                        <span class="input-group-text bg-light border-end-0">
                            <i class="fas fa-user text-secondary"></i>
                        </span>
                        <input type="text" class="form-control border-start-0" id="username" name="username" placeholder="VD: quantri01" required>
                    </div>
                </div>

                <div class="mb-3">
                    <label for="password" class="form-label fw-semibold">Mật khẩu</label>
                    <div class="input-group input-group-lg">
                        <span class="input-group-text bg-light border-end-0">
                            <i class="fas fa-lock text-secondary"></i>
                        </span>
                        <input type="password" class="form-control border-start-0" id="password" name="password" placeholder="Nhập mật khẩu của bạn" required>
                    </div>
                </div>

                <div class="d-flex flex-column flex-md-row align-items-md-center justify-content-md-between mb-4 gap-3">
                    <div class="form-check">
                        <input type="checkbox" class="form-check-input" id="remember" name="remember" value="1">
                        <label class="form-check-label" for="remember">Ghi nhớ đăng nhập</label>
                    </div>
                    <a href="/auth/forgot-password" class="text-decoration-none fw-semibold text-primary">Quên mật khẩu?</a>
                </div>

                <div class="d-grid">
                    <button type="submit" class="btn btn-primary btn-lg">
                        <i class="fas fa-sign-in-alt me-2"></i>
                        Đăng nhập
                    </button>
                </div>
            </form>

            <div class="mt-4 pt-3 border-top text-center">
                <span class="text-muted">Chưa có tài khoản?</span>
                <a href="/auth/register" class="text-decoration-none fw-semibold ms-1">Đăng ký ngay</a>
            </div>

            <div class="mt-4 small text-muted text-center">
                <i class="fas fa-shield-alt me-1"></i>Phiên đăng nhập của bạn được bảo vệ bởi chuẩn SSL và kiểm soát truy cập chặt chẽ.
            </div>
        </div>
    </div>
</div>

<%- contentFor('css') %>
<style>
    .auth-page {
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 20px;
        padding-inline: 12px;
    }

    .auth-alerts {
        width: min(520px, 100%);
        align-self: center;
    }

    body.auth-layout {
        background: radial-gradient(circle at top left, rgba(102, 126, 234, 0.15), transparent 40%),
                    radial-gradient(circle at bottom right, rgba(118, 75, 162, 0.18), transparent 45%),
                    linear-gradient(135deg, #eef2ff 0%, #f8fafc 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 40px 16px;
    }

    .auth-wrapper {
        width: 100%;
        max-width: 1000px;
    }

    .auth-card {
        background: #ffffff;
        border-radius: 28px;
        overflow: hidden;
        display: grid;
        grid-template-columns: minmax(0, 480px) minmax(0, 1fr);
        position: relative;
    }

    .auth-side {
        background: linear-gradient(135deg, #4f46e5, #6d28d9);
        padding: 48px;
        position: relative;
        isolation: isolate;
    }

    .auth-side::after {
        content: '';
        position: absolute;
        inset: 24px;
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 24px;
        opacity: 0.7;
        z-index: -1;
    }

    .auth-side-inner {
        max-width: 320px;
    }

    .auth-form {
        background: #ffffff;
    }

    .auth-form .form-control {
        border-radius: 16px;
        padding: 0.85rem 1rem;
    }

    .auth-form .input-group-text {
        border-radius: 16px 0 0 16px;
    }

    .auth-form .form-control:focus {
        box-shadow: 0 0 0 0.2rem rgba(79, 70, 229, 0.15);
        border-color: rgba(79, 70, 229, 0.6);
    }

    .auth-form .btn-primary {
        border-radius: 16px;
        background: linear-gradient(135deg, #4f46e5, #6366f1);
        border: none;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .auth-form .btn-primary:hover {
        transform: translateY(-1px);
        box-shadow: 0 15px 35px -12px rgba(79, 70, 229, 0.4);
    }

    @media (max-width: 992px) {
        body.auth-layout {
            padding: 32px 12px;
        }

        .auth-card {
            grid-template-columns: 1fr;
        }

        .auth-form {
            padding: 32px 24px;
        }
    }

    @media (max-width: 576px) {
        body.auth-layout {
            padding: 24px 12px;
        }

        .auth-form .input-group {
            flex-direction: column;
        }

        .auth-form .input-group-text {
            width: 100%;
            border-radius: 14px;
            margin-bottom: 6px;
        }

        .auth-form .form-control {
            border-radius: 14px;
        }
    }
</style>

<%- contentFor('js') %>
<script>
document.addEventListener('DOMContentLoaded', function(){
    document.body.classList.add('auth-layout');
});
</script>