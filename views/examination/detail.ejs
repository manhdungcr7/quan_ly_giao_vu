<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> - <%= appName || 'Quản lý Giáo vụ' %></title>
  <link rel="stylesheet" href="/css/layout.css">
  <link rel="stylesheet" href="/css/dashboard.css">
  <link rel="stylesheet" href="/css/documents.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
</head>
<body>
  <%- include('../partials/navbar') %>
  <div class="container-fluid">
    <div class="row">
      <%- include('../partials/sidebar') %>
      <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
        <div class="d-flex justify-content-between align-items-center pt-3 pb-2 mb-3 border-bottom">
          <h1 class="h2"><i class="fas fa-clipboard-check"></i> Chi tiết ca thi</h1>
          <div>
            <a href="/examination/<%= session.id %>/edit" class="btn btn-sm btn-primary"><i class="fas fa-edit"></i> Sửa</a>
            <a href="/examination" class="btn btn-sm btn-secondary"><i class="fas fa-arrow-left"></i> Quay lại</a>
          </div>
        </div>

        <div class="card">
          <div class="card-body">
            <div class="row">
              <div class="col-md-6">
                <h5>Thông tin chung</h5>
                <p><strong>Mã ca thi:</strong> <%= session.exam_code || '-' %></p>
                <p><strong>Tên ca thi:</strong> <%= session.exam_name || '-' %></p>
                <p><strong>Môn học:</strong> <%= session.subject_code %> - <%= session.subject_name %></p>
                <p><strong>Lớp:</strong> <%= session.class_code || '-' %></p>
                <p><strong>Ngày/Giờ thi:</strong> <%= session.exam_date ? new Date(session.exam_date).toLocaleDateString('vi-VN') : '-' %> <%= session.exam_time ? session.exam_time.substring(0,5) : '' %></p>
                <p><strong>Thời lượng:</strong> <%= session.duration || '-' %> phút</p>
              </div>
              <div class="col-md-6">
                <h5>Chấm thi</h5>
                <p><strong>Cán bộ chấm:</strong> <%= session.grader_name || '-' %></p>
                <p><strong>Hạn chấm:</strong> <%= session.grading_deadline ? new Date(session.grading_deadline).toLocaleDateString('vi-VN') : '-' %></p>
                <p><strong>Trạng thái:</strong> <%= session.status %></p>
                <p><strong>Ghi chú:</strong> <%= session.notes || '-' %></p>
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>
  <%- include('../partials/footer') %>
  <script src="/js/app.js"></script>
</body>
</html>
